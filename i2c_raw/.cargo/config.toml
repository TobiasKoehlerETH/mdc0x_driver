[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip STM32G030C8Tx"

[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "info"
DEFMT_RTT_BUFFER_SIZE = "64"
